"""Shared utilities for configuration, dependency checks, and smoke examples.

The package initializer intentionally avoids eager imports so that optional
dependency helpers can be imported without triggering unrelated modules.
"""
